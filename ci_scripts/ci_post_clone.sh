#!/bin/sh
# Xcode Cloud: post-clone script (optional).
# Runs after the repo is cloned. Use for dependencies or env setup if needed.
set -e
# No extra setup required for this project
echo "ci_post_clone: done"
